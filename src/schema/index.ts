import { loginSchema, signupSchema } from "./auth"
import { leadSchema, updateLeadSchema } from "./lead"
import { studentSchema, updateStudentSchema } from "./student"
import { followUpSchema, updateFollowUpSchema } from "./followUp"

export { loginSchema, signupSchema, leadSchema, updateLeadSchema, studentSchema, updateStudentSchema, followUpSchema, updateFollowUpSchema }
